[project]
name = "home-api"
version = "0.1.0"
description = "FastAPI server for my house's IoT stuff"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi[standard]>=0.133.0",
    "pywizlight>=0.6.3",
]
